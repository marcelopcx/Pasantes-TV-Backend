meta {
  name: Eliminar categoría
  type: http
  seq: 4
}

delete {
  url: {{baseURL}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "id": "1"
  }
}

vars:pre-request {
  baseUrl: https://pasantes-tv-backend-production.up.railway.app
}

tests {
  test("status 200", function () {
    expect(res.status).to.equal(200);
  });
}

docs {
  `DELETE /categories` con body `{ "id": 1 }`.
}
