meta {
  name: Eliminar canal
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/channels
  body: json
  auth: inherit
}

body:json {
  {
    "id":"31"
  }
}

vars:pre-request {
  baseUrl: https://pasantes-tv-backend-production.up.railway.app
}

tests {
  test("status 200", function () {
    expect(res.status).to.equal(200);
  });
}

docs {
  `DELETE /channels` con body `{ "id": 31 }`.
}
